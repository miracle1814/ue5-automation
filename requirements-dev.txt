# Offline test suite (no editor / no UE required)
pytest>=8.0
pytest-timeout>=2.3
# ue5_bridge imports these at module level (server stack)
pydantic>=2.0
fastapi>=0.100
uvicorn>=0.23
# httpx: fastapi TestClient (starlette.testclient) dependency
httpx>=0.27
# used by init.py / mermaid_render.py
requests>=2.32.3
PyYAML>=6.0
